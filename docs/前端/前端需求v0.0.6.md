# Vue3 + Ant Design Vue 前端需求文档 v0.0.6

## 1. 文档信息
- 版本：`v0.0.6`
- 对应后端基线日期：`2026-03-14`
- 上一版本：`/Users/lingyunxieqing/Desktop/gos/docs/前端/前端需求v0.0.5.md`
- 本版主题：`登录页 + 用户管理 + 权限控制 + 应用负责人选择`

## 2. 版本目标
新增“用户管理模块”并完成权限驱动前端展示，达成：
- 新增登录页面与登录态管理（登录/退出/会话恢复）
- 应用创建时可下拉选择负责人用户
- 区分管理员与普通用户前端视图与操作权限
- 普通用户仅展示/可操作已授权能力
- 细化到发布单创建页参数级展示与编辑控制

## 3. 范围与边界
包含：
- 登录页（未登录访问拦截）
- 用户管理页面（管理员）
- 权限授权页面（管理员）
- 应用创建/编辑负责人下拉
- 菜单与按钮级权限控制
- 发布参数字段级展示/禁用控制

不包含：
- SSO 登录页 UI
- 组织架构树、岗位体系
- 审批流前端流程

## 4. 菜单与路由增量

### 4.1 菜单结构
- 新增一级菜单：`系统管理`
- 二级菜单：
- `用户管理`
- `权限授权`

### 4.2 新增路由
- `/login`：登录页
- `/system/users`：用户列表页
- `/system/users/:id`：用户详情页（可选）
- `/system/permissions`：权限授权页

### 4.3 菜单显示规则
- 登录页不展示左侧菜单与业务布局壳
- 管理员：显示全部菜单
- 普通用户：仅显示已授权模块菜单；未授权模块不展示（非仅禁用）

## 5. 用户与权限前端模型

### 5.1 用户对象
- `id`
- `username`
- `display_name`
- `email`
- `phone`
- `role`（`admin | normal`）
- `status`（`active | inactive`）

### 5.2 权限对象
- `permission_code`
- `module`
- `action`
- `scope_type`
- `scope_value`

### 5.3 参数级权限对象
- `param_key`
- `application_id`（可空）
- `can_view`
- `can_edit`

### 5.4 当前用户上下文
- `GET /me` 返回：
- 当前用户基础信息
- 角色
- 授权权限列表
- 参数级权限快照（可按需懒加载）

## 6. 页面需求

### 6.1 登录页（新增）
- 表单字段：
- `username`（必填）
- `password`（必填）
- 基本交互：
- 点击登录后调用 `POST /auth/login`
- 登录成功后拉取 `GET /me`，初始化权限并跳转首页
- 登录失败展示后端错误（不暴露敏感细节）
- 补充交互：
- 支持回车提交
- 登录中按钮 loading，避免重复提交
- 风格要求（必须）：
- 需与现有管理后台风格一致（色系、圆角、阴影、按钮规范）
- 遵循样式规范目录：`/Users/lingyunxieqing/Desktop/gos/docs/样式规范/`
- 避免与当前系统视觉割裂（不可单独换一套 UI 语言）

### 6.2 用户管理页（管理员）
- 查询条件：用户名、姓名、角色、状态
- 表格字段：用户名、姓名、角色、状态、邮箱、更新时间、操作
- 操作：新增、编辑、停用/启用、删除（按后端能力）
- 表单字段：`username`、`display_name`、`role`、`status`、`email`、`phone`

### 6.3 权限授权页（管理员）
- 用户选择区：先选用户
- 权限勾选区：
- 模块权限（应用/管线/发布/组件/系统）
- 动作权限（view/create/update/delete/execute/cancel/grant）
- 参数级授权区（重点）：
- `param_key` 下拉（来源标准字库）
- `application_id` 可选
- `can_view`、`can_edit` 开关
- 支持保存、撤销授权

### 6.4 应用创建/编辑页变更
- 负责人字段从输入改为下拉选择
- 数据源：`GET /users/options`
- 提交字段：`owner_user_id`
- 展示字段：`owner`（显示负责人名称）

### 6.5 发布单创建页（重点）
- 参数列表使用后端返回参数（已含权限过滤结果）
- 对每个参数按权限渲染：
- 无 `can_view`：前端不展示
- `can_view=1, can_edit=0`：只读控件（禁用态）
- `can_edit=1`：可编辑
- 必填且只读参数：
- 若后端已给默认值，直接展示默认值
- 若无默认值，展示风险提示并阻止提交（提示联系管理员授权）
- 提交时仅提交可编辑参数与允许提交字段

## 7. 交互与 UX 约束
- 未登录访问受控路由：
- 统一跳转 `/login`
- 未授权访问路由：
- 直接跳转 `403` 页面或首页并提示“未授权”
- 未授权按钮：
- 不展示优先；必要时展示禁用态并带提示
- 权限变更后：
- 前端需刷新当前用户权限缓存（重新拉取 `/me`）
- 全局指令建议：
- `v-permission`（菜单/按钮/区块级）
- `v-param-permission`（发布参数字段级）

## 8. API 对接清单

### 8.0 认证模块
- `POST /auth/login`
- `POST /auth/logout`
- `GET /me`

### 8.1 用户模块
- `GET /users`
- `GET /users/{id}`
- `POST /users`
- `PUT /users/{id}`
- `DELETE /users/{id}`
- `GET /users/options`

### 8.2 权限模块
- `GET /permissions`
- `GET /users/{id}/permissions`
- `POST /users/{id}/permissions`
- `DELETE /users/{id}/permissions`
- `GET /users/{id}/param-permissions`
- `POST /users/{id}/param-permissions`
- `PUT /users/{id}/param-permissions/{permission_id}`
- `DELETE /users/{id}/param-permissions/{permission_id}`

### 8.3 上下文与业务联动
- `POST /applications`（新增 `owner_user_id`）
- `PUT /applications/{id}`（支持 `owner_user_id`）
- `GET /applications/{id}/executor-param-defs`（返回 `can_view/can_edit`）

## 9. 状态管理与缓存
- 新增 `auth store`（Pinia）：
- 保存当前用户、角色、权限点
- 保存登录 token（建议内存 + 本地存储兜底）
- 提供 `hasPermission(code)`、`canViewParam(paramKey, appId)`、`canEditParam(...)`
- 首屏加载策略：
- 登录后先拉 `/me`
- 成功后再渲染动态菜单
- 默认不依赖 Redis，前端权限展示以 `/me` 返回结果为准

## 10. 验收标准（v0.0.6）
- 管理员可在前端完成用户 CRUD 与授权
- 创建应用时负责人可下拉选择用户并保存成功
- 普通用户仅能看到并操作已授权模块
- 发布单创建页参数展示/编辑行为符合授权规则
- 未授权访问受控页面与操作有统一提示与拦截

## 11. 里程碑建议
- M1：用户管理页面与 API 对接
- M2：权限授权页面与权限缓存
- M3：菜单/按钮级权限接入
- M4：发布参数页面字段级权限接入

## 12. Redis 可选增强项对前端影响（非必选）
- 前端无需新增专属 Redis 接口，仍通过 `POST /auth/login`、`POST /auth/logout`、`GET /me` 工作。
- 若后端启用 Redis 会话与权限缓存，前端行为不变，只会体现在登录与鉴权响应更快。
- 权限变更后的前端策略保持不变：关键操作后刷新 `/me`，确保菜单与按钮状态及时更新。

## 13. 当前进度补充（2026-03-14）

### 13.1 登录与权限框架已落地
- 已完成 `/login` 登录页、登录态恢复、退出登录与路由守卫接入。
- 已完成 `auth store`：
- 保存当前用户、权限点、参数权限快照与 token。
- 提供 `hasPermission`、`hasApplicationPermission`、`canViewParam`、`canEditParam` 等能力。
- 已完成未登录重定向与登录页独立布局处理。

### 13.2 系统管理模块已完成
- 已完成 `用户管理` 页面基础 CRUD 对接。
- 已完成 `权限授权` 页面：
- 模块权限勾选与统一保存。
- 应用级发布权限授权（按应用授予 `release.create / release.execute / release.cancel`）。
- 参数级权限 CRUD（平台标准 Key + 应用范围 + `can_view/can_edit`）。
- 当前权限授权页已隐藏过时权限项：`release.param_config.view`。

### 13.3 应用管理与负责人绑定已完成
- 已完成应用创建/编辑负责人下拉选择，负责人数据源来自用户选项接口。
- 已完成“我的应用”菜单与页面，普通用户仅展示有权限访问的应用页能力。
- 已完成应用列表、应用详情、管线绑定页的发布按钮权限控制。

### 13.4 发布管理当前前端现状
- 已新增 `发布模板` 页面：
- 支持按 `应用 + 管线绑定` 维护模板。
- 模板参数来源于已映射平台标准 Key 的真实执行器参数。
- 已完成新建发布单页重构：
- 基础字段当前仅保留：`应用`、`管线选择`、`触发方式`、`备注`、`触发人（只读自动展示）`。
- 不再手动选择发布模板，而是根据 `应用 + 管线选择` 自动匹配启用中的模板。
- 额外参数会自动出现在创建页，并且展示/填写方式已调整为与基础字段一致的表单项形态，不再使用表格录入。
- 已支持额外参数加载动效：加载过程中显示 `Spin`，并禁用“创建发布单”按钮。
- 若当前应用或绑定未配置启用中的发布模板，则：
- 创建页会提示未命中模板。
- “我的应用”/应用详情/管线绑定页的发布入口会置灰不可点击。

### 13.5 发布交互与可用性增强已完成
- 发布按钮已按“应用发布权限 + 模板存在”双条件控制。
- 创建发布单页面在权限变更后可重新拉取最新用户上下文，避免旧权限快照误判。
- 额外参数字段已去掉冗余辅助说明文案，表单展示更接近基础字段录入体验。

### 13.6 当前已验证项
- 已执行：`cd /Users/lingyunxieqing/Desktop/gos/frontend && npm run build`
- 已验证：登录、菜单权限、权限授权、应用负责人下拉、发布模板页、新建发布单页可编译通过。

### 13.7 当前限制说明
- 发布模板当前仍通过独立页面维护，不支持在应用页内联快速创建模板。
- “我的应用”页的发布按钮是否可点击，当前按“应用下存在任一启用模板”判断；真正到绑定级别的校验以管线绑定页与创建页为准。
- 前端尚未补充按钮禁用态的悬浮提示文案（如“当前应用未配置启用中的发布模板”）。

## 14. 当前进度补充（2026-03-15）

### 14.1 应用权限模型已收敛
- 权限授权页原“应用发布权限”已调整为“应用权限”。
- 当前一个应用只保留一个授权开关：`允许展示与发布`。
- 前端保存时会统一授予该应用的：
- `application.view`
- `release.view`
- `release.create`
- `release.execute`
- `release.cancel`
- 当前已去掉“能展示但不能发布”与“能发布但不能展示”的双开关分离模式。

### 14.2 “我的应用”与发布入口已按应用权限收口
- “我的应用”页面当前只展示当前用户被授予应用权限的应用。
- 应用列表、应用详情、管线绑定页中的发布按钮已按“应用权限 + 启用中的发布模板是否存在”双条件控制。
- 未命中模板或未授予应用权限时，发布按钮保持灰色不可点击。
- 管理员仍可查看全部应用，但应用发布入口同样遵循“模板存在”前置条件。

### 14.3 发布单前端可见性已与权限模型对齐
- 普通用户当前只能查看“自己创建且自己有应用权限”的发布单。
- 发布单列表、详情、参数、步骤、日志与阶段日志入口均已按该规则适配。
- 发布单页面中原“触发人”展示语义已切换为“创建者”。
- 新建发布单页面的触发方式已固定为 `manual`，不再允许手动切换。
- 发布单列表与详情中的触发方式文案已统一为中文展示。

### 14.4 新建发布单页面联动已补齐
- 新建发布单页面选择应用后，普通用户已可按当前应用权限正常加载“管线选择”，不再误报 `forbidden`。
- 发布模板仍按“应用 + 管线选择”自动匹配，不再手选模板。
- 额外参数当前已与基础字段采用相同的表单项形态展示与填写，不再单独使用表格区块。
- 额外参数加载时已增加显式动效，加载完成前会禁用创建按钮。

### 14.5 发布详情页已新增 Jenkins 管线进度模块
- 发布详情页当前已新增“管线进度”模块。
- 当绑定的执行器为 Jenkins 时，会展示对应的管线阶段进度与阶段状态。
- 当前阶段模块支持：
- 展示阶段名称、状态、开始时间、结束时间、耗时。
- 点击“查看日志”后打开阶段日志抽屉。
- 目前前端已为后续 `CI / CD / CI+CD` 双分组展示预留结构。

### 14.6 日志与阶段刷新体验已优化
- 构建日志区域当前改为静默自动重连，自动重连时不再弹出“连接中断”类提示。
- 正常流式同步过程中不再持续显示状态标签，仅在“已结束”或“日志异常”时提示。
- 日志区域已支持“跟随日志”与“回到底部”交互，避免用户查看历史日志时被自动滚动打断。
- 管线进度模块改为静默刷新，不再频繁闪烁 loading。
- 阶段日志抽屉打开时，会减少对阶段列表的刷新打扰。

### 14.7 当前已验证项
- 已执行：`cd /Users/lingyunxieqing/Desktop/gos/frontend && npm run build`
- 已验证：应用权限页、我的应用过滤、发布单列表/详情可见性、新建发布单管线选择、发布详情管线进度与日志区域均可编译通过。
