# 抽屉规范

## 使用场景
- 详情信息展示优先使用 `a-drawer`
- 不打断主列表操作流程，避免多级弹窗

## 当前宽度基线
- 绑定详情：`640`
- 标准字段详情：`640`
- 管线参数详情：`720`

## 内容规范
- 加载态优先 `a-skeleton`
- 详情结构统一 `a-descriptions`（推荐 `bordered`）
- 长文本（如原始 JSON）使用 `pre` 并允许换行

## 行为规范
- 关闭后清理详情数据状态，避免脏数据残留
