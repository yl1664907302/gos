# 响应式规范

## 全局断点
- `<=1440`：全局间距保持 `24`
- `<=1024`：全局间距 `20`
- `<=768`：全局间距 `16`，标题字号 `18`

## 页面响应式约束
- 头部区在小屏改为纵向布局并左对齐
- 查询区在小屏可换行或改为单列
- 关键选择框在小屏建议 `width: 100%`
- 分页区可根据页面居中

## 布局响应式约束
- 侧边栏宽度：
- `220`（桌面）
- `200`（<=1024）
- `180`（<=768）
- 内容区内边距：
- `24`（桌面）
- `20`（<=1024）
- `16`（<=768）
