# 可控范围真实执行修复验证报告（2026-04-07）

- 验证时间：2026-04-07 11:14:30
- 验证目的：确认修复 `pipeline binding not found` 之后，发起人可以在可控范围内真实执行自己的发布单，并让发布链路继续推进到 CI 成功收口
- 修复点：`/Users/lingyunxieqing/Desktop/gos/internal/application/usecase/release_order.go`
- 修复策略：当发布单执行快照里已经保存了 `pipeline_id` 时，即使原始 `pipeline binding` 已被删除，也允许回退到快照中的 `pipeline_id` 继续执行

## 1. 测试账号
- 用户名：`real_exec_fix_111430`
- 角色：普通用户
- 应用权限：
  - `application.view`
  - `release.create`
  - `release.cancel`

## 2. 测试对象
- 模板：`南通后端-测试-03`
- 模板 ID：`rt-ca3fc9a410600d1cfae5ee16`
- 应用：`南通后端-测试`
- 环境：`dev`
- 发布单号：`RO-20260407031430-953639CD`
- 发布单 ID：`ro-5af4c60cfca99e8bec8fd561`

## 3. 真实执行结果
- 执行接口返回：`deploying`
- +5s：CI 执行单元进入 `running`，已拿到 Jenkins `queue_url`
- +15s：CI 执行单元拿到 Jenkins `build_url`
- +25s：发布单收口到 `deploy_success`

## 4. 关键证据
- Jenkins Queue：`http://192.168.1.208:8000/queue/item/13030/`
- Jenkins Build：`http://192.168.1.208:8000/job/cd-test/8/`
- 最终 business_status：`deploy_success`
- 最终 raw status：`success`
- CI 执行单元状态：`success`

## 5. 结论
- 发起人真实执行自己发布单：`通过`
- 修复后的快照回退逻辑：`通过`
- 本次验证已经证明：旧的模板绑定记录失效时，只要发布单执行快照里有可用的 `pipeline_id`，仍然可以继续真实触发 Jenkins 管线并成功收口
